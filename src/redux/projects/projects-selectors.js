export const getProjectArray = ({projects}) => projects.projects;

export const getPeople = ({projects}) => projects.project.participants;

export const getProjectInfo = ({projects}) => projects.project;